# GitHub Actions CI Workflow Template
# Generated by /onboard scaffold-expanded
#
# TODO: Customize this template for your project.
# All values below are placeholders — review and update before enabling.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# SEC-002: Minimum required permissions
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # TODO: Set your Python version
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.10'

      # TODO: Install dependencies
      # - run: pip install mcp

      # TODO: Add your lint command
      # - name: Lint
      #   run: flake8 .

      # TODO: Add your test command
      # - name: Test
      #   run: pytest

      # TODO: Add your build command
      # - name: Build
      #   run: echo "No build step configured"
